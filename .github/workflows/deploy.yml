name: Node.js CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build & Test Node App
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run Tests
        run: npm test

      - name: Run Lint
        run: npm run lint

      - name: Upload project
        uses: actions/upload-artifact@v4
        with:
          name: node-project-files
          path: .

  docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/download-artifact@v4
        with:
          name: node-project-files
        
      - name: Login to Docker Hub 
        uses: docker/login-action@v3 
        with: 
          username: ${{ secrets.DOCKER_USERNAME }} 
          password: ${{ secrets.DOCKER_PASSWORD }} 

      - name: Build & Push Docker image 
        run: | 
          docker build -t ${{ secrets.DOCKER_USERNAME }}/node-api-app:latest . 
          docker push ${{ secrets.DOCKER_USERNAME }}/node-api-app:latest 

  security:
      needs: docker 
      runs-on: ubuntu-latest
      if: github.ref == 'refs/heads/main'

      steps:
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ secrets.DOCKERHUB_USERNAME }}/node-api-app:latest'
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'







